[pytest]
testpaths = tests
addopts = -q --strict-markers
markers =
    requires_pygame: needs pygame installed; skipped otherwise
    requires_track: needs the full-size track border images on disk
    slow: excluded from the default CI run
filterwarnings =
    ignore::DeprecationWarning:pygame.*
